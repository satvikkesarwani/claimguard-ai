# ClaimGuard AI — frontend configuration (copy to .env; optional)

# Override the API base. By default the Vite dev server proxies /api -> :8000,
# so this is only needed when calling a non-proxied backend (e.g. a deployed API).
# VITE_API_BASE=http://localhost:8000
